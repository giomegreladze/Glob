# Glob Web Parsing

## Setup

1. Create a `.env` file in the project root for credentials, based on `.env.example`:

```env
EMAIL="your-email@example.com"
PASSWORD="your-password"
```

2. Run the script:

```bash
python main.py
```
